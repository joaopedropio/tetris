{
  "name": "tetris",
  "version": "1.0.0",
  "main": "index.html",
  "scripts": {
    "clean": "shx rm -rf dist",
    "build": "npm run clean && tsc && shx cp favicon.ico tetris.mp3 style.css index.html package.json dist",
    "dev": "npm run build && http-server dist",
    "prod": "shx rm -rf out && npm run build && nwbuild -p linux64 -o out dist && ./out/tetris/linux64/tetris &"
  },
  "devDependencies": {
    "http-server": "^0.12.3",
    "nw-builder": "^3.5.7",
    "shx": "^0.3.3"
  }
}
